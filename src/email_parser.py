def parse_email(email_content):
    """
    Parses email content into structured data (dates, names, tasks).

    Args:
        email_content (str): The content of the email to be parsed.

    Returns:
        dict: A dictionary containing extracted dates, names, and tasks.
    """

parse_email()